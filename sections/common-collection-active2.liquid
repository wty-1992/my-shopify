 
{% if section.settings.enable %}
{% assign swiperId = "swiper-" | append: section.id %} 
 
<style>
  .active-top-box{ height:84px }
  .active-top.stick-sub{
    position: fixed;
    top:70px;
    z-index: 2;
  }   
  .active-top{background:{{ section.settings.color_bg }};}
.pages .common-collection-section .left .swiper-container .swiper-slide a,.pages .countdown-section,.pages .common-collection-section .left .swiper-container .swiper-slide a.active{color:{{ section.settings.color_font }}}
.pages .common-collection-section .active-middle .left ul li:after,.pages .nav-link.active span:after,.common-collection-section .left .swiper-container .swiper-slide:hover span:after{background:{{ section.settings.color_font }};}
.medium-up--btn{display:none}
.common-collection-section  .left .swiper-container{width:100%;}
.common-collection-section  .left .swiper-container .swiper-wrapper{    display: flex;justify-content: flex-end;}
.common-collection-section .left .swiper-container.mb-nav {border-top: 1px solid #fb8b97;}
.common-collection-section .left .swiper-container.mb-nav .swiper-wrapper {
    display: flex;
    justify-content: inherit;
} 
.common-collection-section  .left .swiper-container .swiper-slide{width:auto;padding:0 1rem}
.common-collection-section  .left .swiper-container .swiper-slide a{color:#fff;font-size: 1.1rem;font-weight: 500;}
.common-collection-section  .left .swiper-container .swiper-slide a.active{color:#fff} 
.common-collection-section .active-middle .left.stick-sub{
    position: fixed;
    top: 0px;
    width: 100%;
    z-index: 1; 
    height: 44px;
    display: flex;
    align-items: center;
    padding: 0rem;
    z-index:2;
  background:#e3333f
}
.mb-nav .swiper-slide {
    height: 42px;
    text-align: center;
    overflow: hidden;
    line-height: 30px;
    margin: 0;
}
.mb-nav .swiper-slide a{    display: inline-block;
    height: 100%;
    width: 100%;
    color:#000;
}
.mb-nav .swiper-slide a.active span:after{
	content: "";
    display: block;
    position: absolute;
    width: 100%;
    height: 2px;
    background: #fff;
    left: 0;
    bottom: -3px;
}
.mb-nav  a.active { 
color: #e9024c;
}
.mb-nav  .swiper-{{swiperId}}-mb-button-next2{
    position: absolute;
    right: 0%;
    z-index: 1;
    font-size: 12rem;
    top: 0;
    color: #fff;
    height: 100%;
    display: flex;
    align-items: center;
    width: 5%;
    font-weight: 700; 
    background: #e3333f;
}
.common-collection-section .block-layered-sub .active a{ color:#e60044;}
.common-collection-section .active-middle {
    padding: 0px;
    display: flex;
    flex-direction: column;
}

.common-collection-section .active-middle .left{
    width:100%;
    height: 3.5rem;
    display: flex;
    justify-content: center;
    align-items: center;
    border-bottom: 1px solid #ddd;
 
}
.common-collection-section .active-middle .block-collection {
    background: none;
}
.common-collection-section .active-middle .left ul {
    display: block; 
} 
.common-collection-section .active-middle .left ul li {
    margin-bottom: 0;
    padding: 1rem 1rem 1rem;
    font-size: 1rem;
    cursor: pointer;
    display: inline-block;    position: relative;
  color:#fff;
}.common-collection-section .active-middle .left ul li:after{  
    content: " ";
    position: absolute;
    width: 1px;
    height: 15px;
    right: 0px;
    top: 50%;
    background: #fff;
    display: block;
    transform: translateY(-50%);
}.common-collection-section .active-middle .left ul li:last-child:after{display:none}
.common-collection-section .right .items{
display: flex;
justify-content: flex-start;
align-items: flex-start;
flex-wrap: wrap;
}
.common-collection-section .left .swiper-container .swiper-slide a.active span{position: relative;}
.nav-link.active span:after{
	content: "";
    display: block;
    position: absolute;
    width: 100%;
    height: 2px;
    background: #fff;
    left: 0;
    bottom: -3px;
}
.common-collection-section .items .item .item-content > a {
    display: block;
    padding-bottom: 100%;
    height: 0;
}

.common-collection-section  .collection-detail_list{
    display: grid;
    grid-template-columns: 50% 50%;
    position: relative;
    min-height: 89px;
}
.common-collection-section  .collection-box__price{
    padding:1rem;
    text-align: left;
    display: flex; 
    flex-direction: column;
}
.common-collection-section  .collection-box__sale-price {
    margin-right: .2rem;
    font-size: 1.3rem;
    font-weight: bold;
    color: #000;
}
.common-collection-section  .collection-box__regular-price { 
    color: #ccc;
    font-size: 1rem; 
    text-decoration: line-through;
    margin: .3rem 0 0 0;
}
.common-collection-section  .collection-box__name {
    font-size: 1rem;
    letter-spacing: 1px;
    text-align: left;
    color: #333;
    line-height: 1.2rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    height: 2.3rem;
    padding: 0 1rem; 
    grid-column: 1 / 4;
} .common-collection-section  .collection-box__name.p{color:#e3333f; display:none;
    letter-spacing: 0;}
.common-collection-section .product-card__tag {
     grid-row: 1;
    grid-column: 2 /3;
    padding: 1rem 0;
}
.common-collection-section .common-collection-section .product-card__image-container {
    width: 100%;
    padding-bottom: 0; }
.common-collection-section .common-collection-section .product-card__info{ 
    padding: 0 1rem;
     margin-bottom:1rem;
  width: 100%;
  text-align: center;
  }  
.common-collection-section .collection-off{
    width: 70%;       
    justify-content: center;  	
    color: #fff;
    font-weight: 400;
    font-size: 1.1rem;
    display: -webkit-flex;
    -webkit-justify-content: center;
    -webkit-align-items: center;
    display: flex;
    justify-content: center;
      grid-row: 1;
    grid-column: 2/3;
  } .common-collection-section .collection-label {
    grid-column: 1 /4;
    grid-row: 3;
    align-items: flex-start;
    display: flex;
    padding: .5rem;
    text-align: center;
    position: absolute;
    left: 10px;
    bottom: 8px;
}
.common-collection-section .collection-label .product-label__icon{
    color: #f0a635;
    border: 1px solid #fec369;
    padding:   .2rem .3rem;
    -webkit-font-smoothing: initial;
}
.common-collection-section .btn, .common-collection-section .ratings-fa-star{  
     grid-column: 1 / 4; 
    padding: 0 1rem;
}
 .common-collection-section .ratings-fa-star .yotpo.bottomLine .yotpo-bottomline .yotpo-icon-star{
  font-size: 12px;
}
 .common-collection-section .ratings-fa-star .yotpo .yotpo-stars{
  margin-right:8px;
}
 .common-collection-section .ratings-fa-star .yotpo span{
  line-height:unset;
}
 .common-collection-section .ratings-fa-star .yotpo .text-m{
  font-size: 12px;
} 
.common-collection-section {
    padding:0px;
}
.common-collection-section .items .item{
    width:50%;
} 
.common-collection-section .items .item:nth-child(2n) .item-content{
    margin-right:0px;

}
.common-collection-section .items .item .item-content{    
display: inherit; 
position: relative;
}
.common-collection-section .collection-off span{
	border: 1px solid #dec18c; 
	background: #dec18c;
	padding: 2px 8px;
    display: inline-block;
}
.active-top-box{ height:56px }
.common-collection-section .btn, .common-collection-section .ratings-fa-star{  
     grid-column: 1 / 4;
    padding: 0 0rem;
}
.common-collection-section .ratings-fa-star{
	padding-left: 10px;		
	height:16px;
}
.common-collection-section .items .item{
  width:32%;
  margin-right: 1%;
}
.collection-label-items {
    margin-left: 10px;
    grid-column: 1 / 4;}
.collection-label-items span{padding:0 .5rem}
@media (min-width:1024px){
  
.common-collection-section .active-middle .left.stick-sub {
    position: initial;
    top: 0px;
    width: auto;
    z-index: 1; 
    height:initial;
    padding:initial
}
.medium-up--btn{display:block}
.common-collection-section .items .item{
    width:22%;
    margin-right:0px;
  	margin-left:3%;
  }
.common-collection-section .collection-box__price{
    padding: 0.53rem 0  0.53rem 10px;
    text-align: left;
    display: flex; 
    flex-direction: initial;
}
.common-collection-section .collection-box__sale-price {
    margin-right: .2rem;
    font-size: 1rem;
    font-weight: bold;
    color: #000
}
.common-collection-section .collection-box__regular-price { 
    color: #ccc
    font-size: 1rem; 
    text-decoration: line-through;
    margin: 0 0 0 .5rem;
}
 
.common-collection-section .collection-label {
	grid-column: 1 / 4;
    padding: .5rem 1rem 1rem 0;
    grid-row: 3;
    position: absolute;
    margin-top: 0rem;}
.common-collection-section .collection-label .product-label__icon{
        padding: .2rem .5rem;
}
.common-collection-section .collection-box__name {
    font-size: 1rem;
    letter-spacing: 1px;
    text-align: left;
    color: #333;
    line-height: 1rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    height:1.4rem;
    padding:0px;
  	padding-left:10px;
  	margin-bottom:0px;
  	margin-top:4px;
}
.common-collection-section .items .item .item-content{
      padding:0.5rem 0 0;
  }
.common-collection-section .item .btn{    
    position: absolute;
    bottom:0px;
    opacity: 0; 
    transition: all .2s;
    padding:1.2rem 0rem;
    border-radius:0px;
    grid-column: 1 / 4;
}
.common-collection-section .item:hover .btn{ 
    opacity: 1; 
    transition: all .2s;
}
.common-collection-section .active-middle {
      padding: 0px;
      display: block;
      flex-direction: unset;  
} 
.common-collection-section .active-middle .left {
       width: 80%;
    margin-top: 0;
    float: left;
    height: inherit;
    border-bottom: none;   
    justify-content: flex-end;
      }
.common-collection-section .active-middle .left.stick-sub{

  }
  .common-collection-section .active-middle .left ul {
    display: flex;
    white-space: unset; 
  }

  .common-collection-section .active-middle .left ul li {
    margin-bottom: 0; 
    padding: 1rem 1rem 1rem;
    font-size: 1rem;
    cursor: pointer;
  }

  .common-collection-section .active-middle .left ul li a {
    display: flex;
    justify-content: space-between;
  }

  .common-collection-section .active-middle .left ul li:hover a,
  .common-collection-section .active-middle .left ul li:active a {
    color: #fff;
  }
.w100{ width:100% !important}
.common-collection-section .active-middle .right {
 	width:100%;
    z-index: 1;
    position: relative;
  	margin-top: 40px;
}
} 
@media(max-width:768px){
 
.active-top-box .countdown-section{ 
}
.common-collection-section .left .swiper-container.mb-nav{
	border-top: 1px solid #fff;
}
.common-collection-section .items .item{
	width: 49%;
}
.common-collection-section .collection-off span{
	text-align: center;
}
.common-collection-section .items .item:hover{
	box-shadow: none;
}
.common-collection-section .ratings-fa-star{
	margin: 10px 0;
}
.common-collection-section .collection-label{
	bottom: 20px;
	left: 6px;	
}
.common-collection-section .collection-off{
	width: 100%;
}

}
</style>

{% unless section.settings.html == blank %}
<div class="html main-title">{{section.settings.html}}</div>
{% endunless %} 

<div class="active-middle ">
  <div class="active-top-box">
<div class="active-top"  data-common-pc="{{section.id}}"> 
  <div class="container">
<div class="countdown-section">
  <span class="ends"> <i class="icon photofont photo-daojishi-shijian"></i></span>
  <div id="remainTime" class="remainTime"></div>     
</div>
 
<script type="text/javascript">
$(function(){
  var now=new Date();
  var end=new Date(2030,9,3,00,00,00);//结束的时间：年，月，日，分，秒（月的索引是0~11）
  /*两个时间相减,得到的是毫秒ms,变成秒*/
  var result=Math.floor(end-now)/1000; 
  
  var interval=setInterval(sub,1000); //定时器 调度对象
  /*封装减1秒的函数*/
  function sub(){
      if (result>1) {
         result = result - 1; 
         var second = buquan(Math.floor(result % 60),2);     // 计算秒 ，取余  
         var minite = buquan(Math.floor((result / 60) % 60),2); //计算分 ，换算有多少分，取余，余出多少秒
   var hour = buquan(Math.floor((result / 3600) % 24),2); //计算小时，换算有多少小时，取余，24小时制除以24，余出多少小时

         //var day = buquan(Math.floor(result / (3600*24)),2);  //计算天 ，换算有多少天


     $("#remainTime").html("<ul>" + "<span class='ends'>ENDS:</span>"+ "<li>" + "<span class='num'>"+ hour+ "</span>" + "<span>hours</span>" + " </li> " + "<li>" +"<span class='num'>"+minite+ "</span> " + "<span>mins</span>" + "</li>" + "<li>" +  "<span class='num'>"+second+ "</span>" + "<span>sec</span>" + "<li>"+"</ul>");
      } else{
          window.clearInterval(interval);//这里可以添加倒计时结束后需要执行的事件 
      }
  };
});
function buquan(num,length){
  var numstr = num.toString();
  var l=numstr.length;
  if (numstr.length>=length) {return numstr;}
    
  for(var  i = 0 ;i<length - l;i++){
    numstr = "0" + numstr;  
   }
  return numstr; 
 }
</script> 
    
  {% if section.settings.enable_nav %}
    <div class="left sidebar" data-common-left="{{section.id}}">
      
      
      <div id="{{swiperId}}" class="swiper-container small--hide swiper-nav">
        <ul class="swiper-wrapper">
          {% for block in section.blocks %}
          {% if block.settings.enable %}
          <li class="swiper-slide">
            <a class="nav-link" href="#section-{{block.id}}">
              <span>{{block.settings.title}}  </span>
            </a>
          </li>
          {% endif %}
          {% endfor %}
        </ul>
      </div>
      
      <div id="{{swiperId}}-mb" class="swiper-container swiper-nav medium-up--hide mb-nav">
          <ul class="swiper-wrapper">
              {% for block in section.blocks %}
              {% if block.settings.enable %}
              <li class="swiper-slide">
                  <a class="nav-link" href="#section-{{block.id}}">
                    <span>{{block.settings.title}}  </span>
                  </a>
              </li>
              {% endif %}
              {% endfor %}
          </ul>
          <div class="direction-shade swiper-{{swiperId}}-mb-button-next2"><i class="icon photofont photo-more1"></i></div>
      </div>
    </div>
  {% endif %}
  </div> 
 </div>
  </div>
  <div class="right container {% unless section.settings.enable_nav%}w100{% endunless %}" data-spy="scroll" data-target="#{{section.id}}" data-offset="200">
    {% for block in section.blocks %}
      {% assign blockId = block.id %}
      {% assign current_collection = collections[block.settings.collection] %}
      {% if block.settings.enable and current_collection.products.size > 0 %}
        <div id="section-{{blockId}}" class="section-content-title section-content {{block.settings.style}}">
          {% if block.settings.before_html %}
          <div class="before_html section-content-title" data-selector="{{section.id}}"><span>{{block.settings.before_html}}</span></div>
          {% endif %}
          <ul class="items">
            {% for item in current_collection.products limit: block.settings.limit %}
              {% assign firstVariant = item.first_available_variant %}
              {% assign position = section.settings.position | default: 0 %}
              {% assign image = item.images[position]%}
            <li class="item"> 
             
              <div class="item-content">
                 <div class="collection-label"> 
                  <span style="display:none">
                  {%- capture labelTitle -%}{%- include 'func-verify_rules_json', json: item.metafields["global"]["label"], key: "title" -%}{{ verify__rules_json_result | strip }}{%- endcapture -%}
                  {% assign labelTitle = labelTitle | strip %}
                  {%- capture labelColor -%}{%- include 'func-verify_rules_json', json: item.metafields["global"]["label"], key: "color" -%}{{ verify__rules_json_result | strip }}{%- endcapture -%}
                  {% assign labelColor = labelColor | strip %}
                  {%- capture labelbgColor -%}{%- include 'func-verify_rules_json', json: item.metafields["global"]["label"], key: "background" -%}{{ verify__rules_json_result | strip }}{%- endcapture -%}
                  {% assign labelbgColor = labelbgColor | strip %}
                    {%- capture borderColor -%}{%- include 'func-verify_rules_json', json: item.metafields["global"]["label"], key: "border" -%}{{ verify__rules_json_result | strip }}{%- endcapture -%}
                  {% assign borderColor = borderColor | strip %}
                    {{labelTitle}}-{{labelColor}}-{{labelbgColor}}-{{borderColor}}
                  </span>
                   
                   {% unless item.metafields["global"]["label"] == blank %}
                  
                    <span 
                      class="product-label__icon" 
                      style="color:{{labelColor | default: '#fff'}};border:1px solid {{borderColor | default: '#e60044 ' }};background:{{labelbgColor | default: '#f0a635'}}" >
                      {{labelTitle}}
                    </span>
                	{% endunless %} 
                </div>
                
               <a href="{{item.url}}">  
              <div class="text"></div>
              
              <!-- <span class="add">+</span>-->
              <picture>
                <img
                    src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="  
                    data-size="auto" 
                    data-src="{{ image | img_url: '300x' }}" 
                    class="lazyload" 
                    alt="{{ image.alt | escape }}"/>
              </picture>
               </a>
            <div class="collection-detail_list">
              <div class="collection-box__price">
                <div class="collection-box__sale-price"><span data-money="{{ firstVariant.price }}"></span></div>
                <div class="collection-box__regular-price"><span data-money="{{ firstVariant.compare_at_price }}"></span></div> 
              </div> 
              <div class="collection-off"><span>{{ firstVariant.compare_at_price | minus: firstVariant.price | times: 100.0 | divided_by: firstVariant.compare_at_price | money_without_currency | times: 100 | replace: '.0', ''}}% OFF</span></div>
              <div class="collection-box__name">{{ item.title }}</div>
                
              <div class="collection-label-items"> 
                {% for tag in item.tags %}
                {% if tag contains 'tag-product-' %}
                {% assign tag_all = tag | remove: 'tag-product-' %}
                {% assign tag_array = tag_all | split: '#' %}
                {% assign tag_name = tag_array | first %}
                {% assign tag_color = tag_array | last %}
                {% assign tag_name_size = tag_name.size| minus: 1 %}
                {% assign tag_name_no_last = tag_name | slice: 0, tag_name_size %}
                {% assign tag_name_res = tag_name_no_last | replace: '-', ' ' %}
                <span class=" span-label-item" style="color:#{{ tag_color | default: '#f0a635' }};border:1px solid #{{tag_color | default: '#f0a635' }}" >
                  {{ tag_name_res }}
                </span> 
                {% endif %}
                {% endfor %}
              </div>

               <div class="ratings-fa-star">
        <div class="yotpo bottomLine" data-product-id="{{ item.selected_or_first_available_variant.sku }}"></div>
      </div>
               
				{%- if item.metafields["global"]["cattribute"] or item.variants.size > 1 -%}
				  {% assign cartText = 'design your own' %}
				{% else %}  
				  {% assign cartText = 'add to cart' %}
				{% endif %}
                <button type="button" class="medium-up--btn btn" onclick='{% if item.metafields.global.cattribute or item.variants.size > 1 %}location.href="{{ item.url | within: collection }}"{% else %}Cart.add({id:"{{firstVariant.id}}"}).then(item => { Dialog.open("#shopify-section-cart-success")}){% endif %}'>{{ cartText }}</button>
            </div>
                </div> 
            </li>
            {% endfor %}
          </ul>
          {% if block.settings.after_html %}
          <div class="after_html">{{block.settings.after_html}}</div>
          {% endif %}
        </div> 
        <style>
          .common-collection-section #{{blockId}}{
            background: {{block.settings.color_bg}};
          }
          .common-collection-section #{{blockId}} .items .item {
            border: 1rem solid {{block.settings.color_bg}};
          }
          .common-collection-section #{{blockId}} .items .item a{
            color: {{block.settings.color_text}};
          }
          .common-collection-section #{{blockId}} .items .product-card__sale-price{
            color:{{block.settings.color_price}};
          }
        </style>
      {% endif %}
    {% endfor %}
    </div>
</div>
{% if section.settings.enable_nav %}
<script type="text/javascript">
  
$(function(){
    if($(window).width()>992){
        var swiper = new Swiper('#{{swiperId}}', {
            slidesPerView: 'auto',
            navigation: {
            	nextEl: '.swiper-{{swiperId}}-button-next2'
            }
        });
        $('body').scrollspy({ target: '#{{swiperId}}',offset: 100 });
        $(window).on('activate.bs.scrollspy', function () {
            var index_num = $('#{{swiperId}}>.swiper-wrapper a').index($('#{{swiperId}}>.swiper-wrapper a.active'));
            swiper.slideTo(index_num, 1000);
        });
        $(window).scroll(function(){
            var scrollTop = $(window).scrollTop();
            var toolbarTop = $(".active-top-box").offset().top;
            var navHeight = $('.menu-nav').innerHeight();
            if(scrollTop >= (toolbarTop - navHeight)){
                $('[data-common-pc="{{section.id}}"]').addClass('stick-sub');
            }else{
                $('[data-common-pc="{{section.id}}"]').removeClass('stick-sub');
            };
            $('[data-selector="{{section.id}}"]').each(function(){
                var juliheight =  $(this).offset().top - scrollTop;
                if(juliheight <= 0 && ( juliheight + $(this).parents('.section-content').height()) >= 10 ){
                    var oid = "#" + $(this).parents('div').attr("id"); 
                    $("a[data-href='"+oid+"']").parents('li').addClass("active");
                    $("a[data-href='"+oid+"']").parents('li').siblings().removeClass("active");
                }
            })   
        })
        $('[data-common-left="{{section.id}}"] .swiper-wrapper a').click(function(){
            var offset = 98;
            event.preventDefault();
            $($(this).attr('href'))[0].scrollIntoView();
            scrollBy(0, -offset);
        })
    }
    if($(window).width()<=992){
        /*mb nav start*/
        var swiper = new Swiper('#{{swiperId}}-mb', {
            slidesPerView: 'auto',
            navigation: {
                nextEl: '.swiper-{{swiperId}}-mb-button-next2'
            }
        });
        $('body').scrollspy({ target: '#{{swiperId}}-mb',offset: 100 });
        $(window).on('activate.bs.scrollspy', function () {
            var index_num = $('#{{swiperId}}-mb>.swiper-wrapper a').index($('#{{swiperId}}-mb>.swiper-wrapper a.active'));
            swiper.slideTo(index_num, 1000);
        });
        var toolbarTop = $('#{{swiperId}}-mb').offset().top;
        var headerHei = $('header').innerHeight();
        $(window).scroll(function(){
            var scrollTop = $(window).scrollTop();
            if(scrollTop >= (toolbarTop - headerHei)){
                $('[data-common-left="{{section.id}}"]').addClass('stick-sub');
                //$('.stick-sub').parents('.active-middle').css('padding-top',$('.stick-sub').innerHeight());
                $('.stick-sub').css('top',headerHei);
            }else{
                $('[data-common-left="{{section.id}}"]').removeClass('stick-sub');
            }  
        })
        $('.mb-nav a').click(function(){
            var offset = 98;
            event.preventDefault();
            $($(this).attr('href'))[0].scrollIntoView();
            scrollBy(0, -offset);
        })
        /*mb nav end*/
    }
})  
</script>
{% endif %}

{% endif %}
{% schema %}
{
	"name": "通用 - 产品列表 ( 支持分会场 )",
	"class": "common-collection-section",
	"settings": [
    {
        "type": "checkbox",
        "id": "enable",
        "label": "开启/关闭"
    },
    {
        "type": "checkbox",
        "id": "enable_nav",
        "label": "开启会场导航",
        "default": false
    },
    {
      "type": "html",  
      "id": "html",
      "label": "顶部额外的HTML"
    },
	{
      "type":      "color",
      "id":        "color_bg",
      "label":     "背景颜色",
      "default":   "#e3333f"
    },
	{
      "type":      "color",
      "id":        "color_font",
      "label":     "文字颜色",
      "default":   "#fff"
    } 
	],
	"blocks": [
    {
		"type": "common-collection",
		"name": "collection",
		"settings": [
			{
				"type": "checkbox",
				"id": "enable",
				"label": "是否开启此分会场"
			},
      {
				"type": "text",
				"id": "title",
				"label": "分会场标题"
			},
      {
				"type": "html",
				"id": "before_html",
				"label": "分会场右侧标题"
			},
      { 
          	"type": "collection",
				"id": "collection",
				"label": "选择集合"
			},
			{
				"type": "range",
				"id": "limit",
				"label": "产品limit总数量",
				"min": 1,
				"max": 100,
				"step": 1,
				"default": 4
			},
      {
          "type":      "color",
          "id":        "color_bg",
          "label":     "背景颜色",
          "default":   "#000"
      },
			{
          "type":      "color",
          "id":        "color_title",
          "label":     "标题颜色",
          "default":   "#fff"
      },
      {
          "type":      "color",
          "id":        "color_text",
          "label":     "文字颜色",
          "default":   "#fff"
      },
			{
          "type":      "color",
          "id":        "color_price",
          "label":     "价格颜色",
          "default":   "#F70724"
      },
      {
				"type": "html",
				"id": "after_html",
				"label": "分会场底部自定义图片文案"
			}
		]
	}],
	"presets": [{
		"name": "通用 - 产品列表 ( 支持分会场 )",
		"category": "通用"
	}]
}
{% endschema %}

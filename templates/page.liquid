<style>
  {% if handle == '90-day-returns' %}
  .days-return-container ul{
  	list-style: disc;
    padding-left: 1.5em;
  }
  .main-container .catalog-category-bg{
  	margin-bottom: 70px;
  }
  {% endif %}
</style>
{{ page.content }}
